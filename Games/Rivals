--______/\\\\\\\\\\\__/\\\_______/\\\___________________/\\\\\\\\\\\__________/\\\\\\\\\____/\\\\\\\\\______/\\\\\\\\\\\__/\\\\\\\\\\\\\____/\\\\\\\\\\\\\\\_____/\\\\\\\\\\\___        
-- _____\/////\\\///__\///\\\___/\\\/__________________/\\\/////////\\\_____/\\\////////___/\\\///////\\\___\/////\\\///__\/\\\/////////\\\_\///////\\\/////____/\\\/////////\\\_       
--  _________\/\\\_______\///\\\\\\/___________________\//\\\______\///____/\\\/___________\/\\\_____\/\\\_______\/\\\_____\/\\\_______\/\\\_______\/\\\________\//\\\______\///__      
--   _________\/\\\_________\//\\\\_______/\\\\\\\\\\\___\////\\\__________/\\\_____________\/\\\\\\\\\\\/________\/\\\_____\/\\\\\\\\\\\\\/________\/\\\_________\////\\\_________     
--    _________\/\\\__________\/\\\\______\///////////_______\////\\\______\/\\\_____________\/\\\//////\\\________\/\\\_____\/\\\/////////__________\/\\\____________\////\\\______    
--     _________\/\\\__________/\\\\\\___________________________\////\\\___\//\\\____________\/\\\____\//\\\_______\/\\\_____\/\\\___________________\/\\\_______________\////\\\___   
--      __/\\\___\/\\\________/\\\////\\\__________________/\\\______\//\\\___\///\\\__________\/\\\_____\//\\\______\/\\\_____\/\\\___________________\/\\\________/\\\______\//\\\__  
--       _\//\\\\\\\\\_______/\\\/___\///\\\_______________\///\\\\\\\\\\\/______\////\\\\\\\\\_\/\\\______\//\\\__/\\\\\\\\\\\_\/\\\___________________\/\\\_______\///\\\\\\\\\\\/___ 
--        __\/////////_______\///_______\///__________________\///////////___________\/////////__\///________\///__\///////////__\///____________________\///__________\///////////_____
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "LoaderChoice"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = game.CoreGui

local Background = Instance.new("Frame")
Background.Name = "Background"
Background.Size = UDim2.new(1, 0, 1, 0)
Background.Position = UDim2.new(0, 0, 0, 0)
Background.AnchorPoint = Vector2.new(0, 0)
Background.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
Background.BorderSizePixel = 0
Background.ZIndex = 1
Background.Parent = ScreenGui

ScreenGui.IgnoreGuiInset = true

local CloseButton = Instance.new("TextButton")
CloseButton.Name = "CloseButton"
CloseButton.Size = UDim2.new(0, 120, 0, 40)
CloseButton.Position = UDim2.new(0.5, -60, 0, 20)
CloseButton.BackgroundColor3 = Color3.fromRGB(231, 76, 60)
CloseButton.BorderSizePixel = 0
CloseButton.Text = "✕ CLOSE"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.TextSize = 18
CloseButton.Font = Enum.Font.GothamBold
CloseButton.ZIndex = 5
CloseButton.Parent = Background

local CloseButtonCorner = Instance.new("UICorner")
CloseButtonCorner.CornerRadius = UDim.new(0, 10)
CloseButtonCorner.Parent = CloseButton

local Container = Instance.new("Frame")
Container.Name = "Container"
Container.Size = UDim2.new(0, 800, 0, 400)
Container.Position = UDim2.new(0.5, 0, 0.5, 0)
Container.AnchorPoint = Vector2.new(0.5, 0.5)
Container.BackgroundTransparency = 1
Container.Parent = Background

local ListLayout = Instance.new("UIListLayout")
ListLayout.FillDirection = Enum.FillDirection.Horizontal
ListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
ListLayout.VerticalAlignment = Enum.VerticalAlignment.Center
ListLayout.Padding = UDim.new(0, 20)
ListLayout.Parent = Container

local UndetectedBox = Instance.new("Frame")
UndetectedBox.Name = "UndetectedBox"
UndetectedBox.Size = UDim2.new(0, 380, 0, 400)
UndetectedBox.BackgroundColor3 = Color3.fromRGB(40, 180, 99)
UndetectedBox.BorderSizePixel = 0
UndetectedBox.Parent = Container

local UndetectedCorner = Instance.new("UICorner")
UndetectedCorner.CornerRadius = UDim.new(0, 15)
UndetectedCorner.Parent = UndetectedBox

local UndetectedTitle = Instance.new("TextLabel")
UndetectedTitle.Name = "Title"
UndetectedTitle.Size = UDim2.new(1, -40, 0, 60)
UndetectedTitle.Position = UDim2.new(0, 20, 0, 80)
UndetectedTitle.BackgroundTransparency = 1
UndetectedTitle.Text = "UNDETECTED"
UndetectedTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
UndetectedTitle.TextSize = 32
UndetectedTitle.Font = Enum.Font.GothamBold
UndetectedTitle.Parent = UndetectedBox

local UndetectedDesc = Instance.new("TextLabel")
UndetectedDesc.Name = "Description"
UndetectedDesc.Size = UDim2.new(1, -40, 0, 100)
UndetectedDesc.Position = UDim2.new(0, 20, 0, 160)
UndetectedDesc.BackgroundTransparency = 1
UndetectedDesc.Text = "Small Features\n\nSafe & Recommended"
UndetectedDesc.TextColor3 = Color3.fromRGB(230, 230, 230)
UndetectedDesc.TextSize = 18
UndetectedDesc.Font = Enum.Font.Gotham
UndetectedDesc.TextWrapped = true
UndetectedDesc.TextYAlignment = Enum.TextYAlignment.Top
UndetectedDesc.Parent = UndetectedBox

local UndetectedButton = Instance.new("TextButton")
UndetectedButton.Name = "Button"
UndetectedButton.Size = UDim2.new(0, 200, 0, 50)
UndetectedButton.Position = UDim2.new(0.5, -100, 1, -80)
UndetectedButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
UndetectedButton.BorderSizePixel = 0
UndetectedButton.Text = "SELECT"
UndetectedButton.TextColor3 = Color3.fromRGB(40, 180, 99)
UndetectedButton.TextSize = 20
UndetectedButton.Font = Enum.Font.GothamBold
UndetectedButton.Parent = UndetectedBox

local UndetectedButtonCorner = Instance.new("UICorner")
UndetectedButtonCorner.CornerRadius = UDim.new(0, 10)
UndetectedButtonCorner.Parent = UndetectedButton

local DetectedBox = Instance.new("Frame")
DetectedBox.Name = "DetectedBox"
DetectedBox.Size = UDim2.new(0, 380, 0, 400)
DetectedBox.BackgroundColor3 = Color3.fromRGB(231, 76, 60)
DetectedBox.BorderSizePixel = 0
DetectedBox.Parent = Container

local DetectedCorner = Instance.new("UICorner")
DetectedCorner.CornerRadius = UDim.new(0, 15)
DetectedCorner.Parent = DetectedBox

local DetectedTitle = Instance.new("TextLabel")
DetectedTitle.Name = "Title"
DetectedTitle.Size = UDim2.new(1, -40, 0, 60)
DetectedTitle.Position = UDim2.new(0, 20, 0, 80)
DetectedTitle.BackgroundTransparency = 1
DetectedTitle.Text = "DETECTED!!"
DetectedTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
DetectedTitle.TextSize = 32
DetectedTitle.Font = Enum.Font.GothamBold
DetectedTitle.Parent = DetectedBox

local DetectedDesc = Instance.new("TextLabel")
DetectedDesc.Name = "Description"
DetectedDesc.Size = UDim2.new(1, -40, 0, 100)
DetectedDesc.Position = UDim2.new(0, 20, 0, 160)
DetectedDesc.BackgroundTransparency = 1
DetectedDesc.Text = "A Lot Of Features\n\n⚠️ DON'T USE THIS ON YOUR MAIN ACCOUNT!!!"
DetectedDesc.TextColor3 = Color3.fromRGB(255, 230, 230)
DetectedDesc.TextSize = 18
DetectedDesc.Font = Enum.Font.Gotham
DetectedDesc.TextWrapped = true
DetectedDesc.TextYAlignment = Enum.TextYAlignment.Top
DetectedDesc.Parent = DetectedBox

local DetectedButton = Instance.new("TextButton")
DetectedButton.Name = "Button"
DetectedButton.Size = UDim2.new(0, 200, 0, 50)
DetectedButton.Position = UDim2.new(0.5, -100, 1, -80)
DetectedButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
DetectedButton.BorderSizePixel = 0
DetectedButton.Text = "SELECT"
DetectedButton.TextColor3 = Color3.fromRGB(231, 76, 60)
DetectedButton.TextSize = 20
DetectedButton.Font = Enum.Font.GothamBold
DetectedButton.Parent = DetectedBox

local DetectedButtonCorner = Instance.new("UICorner")
DetectedButtonCorner.CornerRadius = UDim.new(0, 10)
DetectedButtonCorner.Parent = DetectedButton

local WarningDialog = Instance.new("Frame")
WarningDialog.Name = "WarningDialog"
WarningDialog.Size = UDim2.new(0.9, 0, 0, 300)
WarningDialog.Position = UDim2.new(0.5, 0, 0.5, -150)
WarningDialog.AnchorPoint = Vector2.new(0.5, 0.5)
WarningDialog.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
WarningDialog.BorderSizePixel = 0
WarningDialog.Visible = false
WarningDialog.Parent = Background
WarningDialog.ZIndex = 10

local SizeConstraint = Instance.new("UISizeConstraint")
SizeConstraint.MaxSize = Vector2.new(500, 300)
SizeConstraint.Parent = WarningDialog

local WarningCorner = Instance.new("UICorner")
WarningCorner.CornerRadius = UDim.new(0, 15)
WarningCorner.Parent = WarningDialog

local WarningTitle = Instance.new("TextLabel")
WarningTitle.Size = UDim2.new(1, -40, 0, 60)
WarningTitle.Position = UDim2.new(0, 20, 0, 20)
WarningTitle.BackgroundTransparency = 1
WarningTitle.Text = "⚠️ FINAL WARNING ⚠️"
WarningTitle.TextColor3 = Color3.fromRGB(255, 200, 0)
WarningTitle.TextSize = 28
WarningTitle.Font = Enum.Font.GothamBold
WarningTitle.Parent = WarningDialog

local WarningText = Instance.new("TextLabel")
WarningText.Size = UDim2.new(1, -40, 0, 100)
WarningText.Position = UDim2.new(0, 20, 0, 90)
WarningText.BackgroundTransparency = 1
WarningText.Text = "This version has HIGH DETECTION RISK!\n\nAre you sure you want to continue?"
WarningText.TextColor3 = Color3.fromRGB(255, 255, 255)
WarningText.TextSize = 18
WarningText.Font = Enum.Font.Gotham
WarningText.TextWrapped = true
WarningText.TextYAlignment = Enum.TextYAlignment.Top
WarningText.Parent = WarningDialog

local CancelButton = Instance.new("TextButton")
CancelButton.Size = UDim2.new(0, 200, 0, 50)
CancelButton.Position = UDim2.new(0, 30, 1, -70)
CancelButton.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
CancelButton.BorderSizePixel = 0
CancelButton.Text = "GO BACK"
CancelButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CancelButton.TextSize = 18
CancelButton.Font = Enum.Font.GothamBold
CancelButton.Parent = WarningDialog

local CancelCorner = Instance.new("UICorner")
CancelCorner.CornerRadius = UDim.new(0, 10)
CancelCorner.Parent = CancelButton

local ContinueButton = Instance.new("TextButton")
ContinueButton.Size = UDim2.new(0, 200, 0, 50)
ContinueButton.Position = UDim2.new(1, -230, 1, -70)
ContinueButton.BackgroundColor3 = Color3.fromRGB(231, 76, 60)
ContinueButton.BorderSizePixel = 0
ContinueButton.Text = "CONTINUE ANYWAY"
ContinueButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ContinueButton.TextSize = 18
ContinueButton.Font = Enum.Font.GothamBold
ContinueButton.Parent = WarningDialog

local ContinueCorner = Instance.new("UICorner")
ContinueCorner.CornerRadius = UDim.new(0, 10)
ContinueCorner.Parent = ContinueButton

local function loadScript(url)
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)
    
    if success then
        local loadSuccess, loadResult = pcall(function()
            return loadstring(result)()
        end)
        
        if loadSuccess then
            ScreenGui:Destroy()
        else
            warn("Failed to load script: " .. tostring(loadResult))
        end
    else
        warn("Failed to fetch script: " .. tostring(result))
    end
end

CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

UndetectedButton.MouseButton1Click:Connect(function()
    loadScript("https://raw.githubusercontent.com/jianlobiano/LOADER/refs/heads/main/Games/Rivals%20Undetected")
end)

DetectedButton.MouseButton1Click:Connect(function()
    WarningDialog.Visible = true
end)

CancelButton.MouseButton1Click:Connect(function()
    WarningDialog.Visible = false
end)

ContinueButton.MouseButton1Click:Connect(function()
    loadScript("https://raw.githubusercontent.com/jianlobiano/LOADER/refs/heads/main/Games/Rivals")
end)

local function addHoverEffect(button, normalColor, hoverColor)
    button.MouseEnter:Connect(function()
        button.BackgroundColor3 = hoverColor
    end)
    
    button.MouseLeave:Connect(function()
        button.BackgroundColor3 = normalColor
    end)
end

addHoverEffect(UndetectedButton, Color3.fromRGB(255, 255, 255), Color3.fromRGB(230, 230, 230))
addHoverEffect(DetectedButton, Color3.fromRGB(255, 255, 255), Color3.fromRGB(230, 230, 230))
addHoverEffect(CancelButton, Color3.fromRGB(100, 100, 100), Color3.fromRGB(120, 120, 120))
addHoverEffect(ContinueButton, Color3.fromRGB(231, 76, 60), Color3.fromRGB(200, 60, 50))
addHoverEffect(CloseButton, Color3.fromRGB(231, 76, 60), Color3.fromRGB(200, 60, 50))
